%nav.navbar.navbar-default.navbar-fixed-top{role: "navigation"}
  .container-fluid
    / Brand and toggle get grouped for better mobile display
    .navbar-header
      %button.navbar-toggle{"data-target" => "#bs-example-navbar-collapse-1", "data-toggle" => "collapse", type: "button"}
        %span.sr-only Toggle navigation
        %span.icon-bar
        %span.icon-bar
        %span.icon-bar
      = link_to "MyPix", root_path, class: "navbar-brand", data: {toggle: "tooltip", placement: "bottom"}, title: "List your pictures"
    #bs-example-navbar-collapse-1.collapse.navbar-collapse
      - if user_signed_in?
        .navbar-form.navbar-left
          = link_to "<span class='glyphicon glyphicon-plus'></span> Upload a Pix".html_safe, new_user_pic_path(current_user), data: {toggle: "tooltip", placement: "bottom"}, title: "Upload a new picture", class: "bs-tooltip btn btn-info navbar-btn btn-sm"
        %ul.nav.navbar-nav.navbar-right
          %li.dropdown
            %a.dropdown-toggle{"data-toggle" => "dropdown", href: "#"}
              %span.glyphicon.glyphicon-user{aria: {hidden: "true"}}
              = current_user.name
              %b.caret
            %ul.dropdown-menu
              %li= link_to "Edit yout account", edit_user_registration_path(current_user)
              %li= link_to "Sign out", destroy_user_session_path
